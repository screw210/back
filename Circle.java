package exercise;

/*public class Circle {

	public static void main(String[] args) {
		//멤버변수
		int r;
		final double PI = 3.14;
		
		//생성자 메서드 =>인스턴스 생성
		public Circle(int r) {
			this.r = r;
		}
		public double getRound() {		//외부에서 값을 가져오면 안된다.
			return (2 * PI * this.r);		//this == 다 자신의 인스턴스 객체 오타를 낸다면 엉뚱한 값을 가져오기 때문에 자신의 것을 써야한다.
		}
		public double getArea() {
			return(PI * this.r * this.r);
		}
	}

}

1차원 배열, 2차원 배열, 클래스*/